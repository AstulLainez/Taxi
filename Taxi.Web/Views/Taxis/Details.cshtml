@model Taxi.Web.Data.Entities.TaxiEntity

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>TaxiEntity</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Plaque)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Plaque)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning"> <i class="glyphicon glyphicon-pencil"></i> Editar</a> 
    <a asp-action="Index" class="btn btn-success"> <i class="glyphicon glyphicon-share-alt"></i> Regresar a la Lista</a>
</div>
